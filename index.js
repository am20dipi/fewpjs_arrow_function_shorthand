let divide = function() {
    let sum = 2000/100
    return sum
}

let square = (num) => {
    let sum = num * num
    return sum
}

let add = (first, second) => {
    let sum = first + second
    return sum
}